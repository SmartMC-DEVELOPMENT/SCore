package us.smartmc.gamesmanager.game.teams;

public class TeamSelector extends EnumSelector<PlayerTeam> {

    /*public void alternate() {
        int teamsAmount = PlayerTeam.values().length; // = 5
        teamSelectedIndex++;
        if (teamSelectedIndex == teamsAmount + 1) { // = 6
            teamSelectedIndex = 0;
        }
    }

    public PlayerTeam getSelected() {
        return PlayerTeam.values()[teamSelectedIndex];
    }*/

}
